# DNGuard-InvalidMD

The easiest way to remove DNGuard Invalid-MD.

This is just a test, purely make the original repo runnable, there is no guarantee it'll work.

## Quickstart

```bash
$ cd src/DNGuardDecoder/
$ dotnet run /Users/dotnet/App.dll
Detected InvalidMD @ RunHVM (1)
Detected InvalidMD @ RunHVM (1)
Output: /Users/dotnet/App_Decoded.dll
```
